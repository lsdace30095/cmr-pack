apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: heatmap-generator-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: heatmap-generator-service
            port:
              number: 8000
