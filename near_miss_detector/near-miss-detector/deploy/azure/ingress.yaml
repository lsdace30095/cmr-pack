apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nmd-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nmd-service
            port:
              number: 8000
